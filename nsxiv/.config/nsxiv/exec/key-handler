#!/bin/sh

while read -r file
do
case "$1" in
	"w")
		setbg "$file" &
		;;
	"r")
		convert -rotate 90 "$file" "$file"
		;;
	"d")
		prompt "Remove this image?" && rm "$file" && notify-send "$file deleted"
		;;
esac
done
