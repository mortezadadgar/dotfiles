#!/bin/sh
# Description: rebuild all gentoo packages
# Shell: POSIX compliant
set -eu

exclude_pkg="
	dev-python/*
	app-alternatives/*
	sys-kernel/*
	sys-firmware/*
	sys-devel/clang
	sys-devel/llvm
	sys-devel/gcc
	net-libs/nodejs
	x11-themes/*
	acct-user/*
	acct-group/*
	media-fonts/*
	virtual/*
	*-bin
"

sudo emerge -e --update --newuse --quiet-build --exclude "${exclude_pkg}" @world "$@"
