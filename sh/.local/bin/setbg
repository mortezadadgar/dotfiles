#!/bin/bash
# Description: set default desktop wallpaper
# Dependencies:swaybg
# Shell: Bash
set -eu -o pipefail

swaybg --image "$1" &
cp -v "$1" "$HOME"/.local/bashare/bg.jpg
mv -v "$1" "$HOME"/Pictures/wallpapers/
