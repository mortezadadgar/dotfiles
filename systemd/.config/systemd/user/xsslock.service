[Unit]
Description=xss-lock
Wants=graphical-session.target
After=graphical-session.target

[Service]
ExecStart=/usr/bin/xss-lock --session ${XDG_SESSION_ID} --transfer-sleep-lock -- slock
Restart=always

[Install]
WantedBy=graphical-session.target
